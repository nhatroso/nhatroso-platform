{
  "name": "nhatroso-platform",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "turbo run dev",
    "dev:web": "turbo run dev --filter=web",
    "dev:mobile": "turbo run dev --filter=mobile",
    "dev:api": "turbo run dev --filter=server-api",
    "build": "turbo run build",
    "build:web": "turbo run build --filter=web",
    "build:mobile": "turbo run build --filter=mobile",
    "build:api": "turbo run build --filter=server-api",
    "lint": "turbo run lint",
    "lint:web": "turbo run lint --filter=web",
    "lint:mobile": "turbo run lint --filter=mobile",
    "lint:api": "turbo run lint --filter=server-api",
    "format": "turbo run format",
    "format:web": "turbo run format --filter=web",
    "format:mobile": "turbo run format --filter=mobile",
    "format:api": "turbo run format --filter=server-api",
    "typecheck": "turbo run typecheck",
    "typecheck:web": "turbo run typecheck --filter=web",
    "typecheck:mobile": "turbo run typecheck --filter=mobile",
    "typecheck:api": "turbo run typecheck --filter=server-api",
    "check": "turbo run check",
    "check:web": "turbo run check --filter=web",
    "check:mobile": "turbo run check --filter=mobile",
    "check:api": "turbo run check --filter=server-api",
    "prepare": "husky"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.30.3",
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^8.56.1",
    "@typescript-eslint/parser": "^8.56.1",
    "eslint": "^10.0.2",
    "eslint-config-prettier": "^10.1.8",
    "eslint-plugin-prettier": "^5.5.5",
    "husky": "^9.1.7",
    "lint-staged": "^16.3.1",
    "prettier": "^3.8.1",
    "turbo": "^2.8.12"
  }
}
